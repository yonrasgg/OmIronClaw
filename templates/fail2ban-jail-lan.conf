# LAN Host — fail2ban (CIS 5.4.2 / NIST SI-4)
# Generic profile — SSH on port 2222

[DEFAULT]
bantime  = 1h
findtime = 10m
maxretry = 5
backend  = systemd
banaction = nftables[type=allports]

[sshd]
enabled  = true
port     = 2222
filter   = sshd
maxretry = 3
bantime  = 2h
